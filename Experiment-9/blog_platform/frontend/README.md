# Frontend (React)

Quick start (assumes Node 16+):
1. Install:
   ```
   cd frontend
   npm install
   ```
2. Start:
   ```
   npm start
   ```
By default the app expects backend at http://localhost:5000
