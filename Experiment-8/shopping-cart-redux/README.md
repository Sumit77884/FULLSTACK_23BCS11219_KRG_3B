# Redux Shopping Cart (Minimal UI)

How to run:

1. Install dependencies
   ```
   npm install
   ```
2. Start dev server
   ```
   npm run dev
   ```
3. Open the URL shown in terminal (usually http://localhost:5173)

This project uses React + Vite + Redux Toolkit and persists cart state to localStorage.