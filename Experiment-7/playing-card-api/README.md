# Playing Card API

A simple REST API built with Express.js to manage a collection of playing cards.

## Run Instructions

```bash
npm install
npm start
```

Endpoints:
- GET /cards
- GET /cards/:id
- POST /cards
- PUT /cards/:id
- DELETE /cards/:id
